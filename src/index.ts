
import { BuilderTarea } from "./--Builder/Tarea/BuilderTarea";
import { DirectorTarea } from "./--Builder/Tarea/DirectorTarea";
import { App } from "./App/App";
import Tarea from "./Tarea/Tarea";

function main() {

    // que me paso aqui? estoy bien?

    
    const builderTarea=new BuilderTarea(maxID);
    const directorTarea=new DirectorTarea(builderTarea);
    app.tareas=new Map();
    const tarea1=(,"Estudiar Typescript");


    app.tareas.set(tarea.i,)
}

main();